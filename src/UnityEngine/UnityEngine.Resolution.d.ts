declare namespace UnityEngine {
  class Resolution extends System.ValueType {
    // constructors
    protected constructor();
    // methods
    ToString(): string;
    // properties
    width: number;
    height: number;
    refreshRate: number;
    // fields
  }
}
