declare namespace UnityEditor {
  class TimeHelper extends System.ValueType {
    // constructors
    protected constructor();
    // methods
    Begin(): void;
    Update(): number;
    // properties
    // fields
    deltaTime: number;
  }
}
