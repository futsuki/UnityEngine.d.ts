declare namespace UnityEditor {
  class ProfilerChoise extends System.ValueType {
    // constructors
    protected constructor();
    // methods
    // properties
    // fields
    Name: string;
    Enabled: boolean;
    IsSelected: (() => boolean);
    ConnectTo: (() => void);
  }
}
