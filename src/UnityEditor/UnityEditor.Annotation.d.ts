declare namespace UnityEditor {
  class Annotation extends System.ValueType {
    // constructors
    protected constructor();
    // methods
    // properties
    // fields
    iconEnabled: number;
    gizmoEnabled: number;
    flags: number;
    classID: number;
    scriptClass: string;
  }
}
