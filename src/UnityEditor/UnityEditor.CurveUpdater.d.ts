declare namespace UnityEditor {
  class CurveUpdater {
    // constructors
    private constructor();
    // methods
    UpdateCurves(curve: any, undoText: string): void;
    // properties
    // fields
  }
}
